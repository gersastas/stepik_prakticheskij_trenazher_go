У вас есть переменные `year`, `month`, `day`, `hour`, `minute`, `second`, которые содержат входные пользовательские данные.

Напишите код, который приводит дату к такому формату `Mon 01-01-2023` и записывает результат в переменную `result`.

**Sample Input:**

```
2023 | 1 | 1 | 2 | 20 | 49
```

**Sample Output:**

```
Sun 01-01-2023
```