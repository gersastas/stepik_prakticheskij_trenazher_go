У вас есть переменная `message`, которая содержит входные пользовательские данные.

Напишите код, который переводит значение строки `message` из `kebab-case` в `lowerCamelCase` и записывает результат в переменную `result`.

**Sample Input:**

```
the-nina-project
```

**Sample Output:**

```
theNinaProject
```