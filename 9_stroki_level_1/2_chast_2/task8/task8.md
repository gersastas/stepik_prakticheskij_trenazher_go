У вас есть переменная `message`, которая принимает входные пользовательские данные.

Напишите код, который подсчитывает количество символов `#` в строке `message` и записывает результат в переменную `result`.

**Sample Input:**

```
###1111###01###
```

**Sample Output:**

```
9
```