У вас есть переменные `x`, `y`, которые содержат входные пользовательские данные.

Напишите код, который делит `x` на `y` и записывает результат в переменную `result`.

**Sample Input:**

```
4 2
```

**Sample Output:**

```
2
```