У вас есть переменная `message`, которая содержит входные пользовательские данные.

Напишите код, который удаляет **(лишнее)** из строки `message` и записывает результат в переменную `result`. 

**Sample Input:**

```
((лишнее))Макс (лишнее(лишнее)(лишнее)(лишнее))бросала меня пять (лишнее(лишнее))раз. Но что я сделал, а? Я отчаялся?(лишнее) Нет. (лишнее)Я снова отправился на (лишнее)битву и завоевывал ее каждый (лишнее(лишнее)(лишнее)(лишнее))чертов раз.(лишнее(лишнее)(лишнее)(лишнее))
```

**Sample Output:**

```
Макс бросала меня пять раз. Но что я сделал, а? Я отчаялся? Нет. Я снова отправился на битву и завоевывал ее каждый чертов раз.
```